repos:
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.6.9
    hooks:
      - id: ruff
        args: [--fix, --unsafe-fixes, --exclude, "*.ipynb"]
      - id: ruff-format
        args: [--exclude, "*.ipynb"]

  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: trailing-whitespace
      - id: end-of-file-fixer
      - id: check-yaml
      - id: check-added-large-files
      - id: check-merge-conflict

# Pydoclint is configured for manual use - run via: uv run pydoclint optimal_cutoffs/
# Commented out from pre-commit to allow baseline workflow for gradual improvement
#  - repo: https://github.com/jsh9/pydoclint
#    rev: 0.5.9
#    hooks:
#      - id: pydoclint
#        args: [--style=numpy, --check-return-types=false, --check-arg-order=false]
#        files: ^optimal_cutoffs/
